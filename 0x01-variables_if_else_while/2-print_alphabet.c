#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 * Return: 0
 */
int main(void)
{
	char dbana;

	for (dbana = 'a'; dbana <= 'z'; dbana++)
		putchar(dbana);
		putchar('\n');
	return (0);
}
